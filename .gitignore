# Build output is now tracked in the repo (kept intentionally), except the
# intermediate the pipeline writes before it is copied to Documentation.docx
build/SRS_Document.docx

# macOS
.DS_Store

# MS Word / LibreOffice temp+lock files
~$*
.~lock.*#

# Temp files
*.tmp

# Python bytecode cache
__pycache__/

# Editor / Obsidian vault config
.obsidian/

# Windows folder metadata
desktop.ini

# Nested product repo (lives at github.com/zainulhassan815/hireflow)
hireflow/
